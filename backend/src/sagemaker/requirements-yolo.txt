# Python dependencies for YOLO-based Lambda function

# Core dependencies
boto3==1.34.0
numpy==1.24.3

# PyTorch (CPU-only for Lambda cost optimization)
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.1.0+cpu
torchvision==0.16.0+cpu

# YOLOv8
ultralytics==8.0.196
dill>=0.3.6

# OpenCV (headless for Lambda)
opencv-python-headless==4.8.1.78

# Image processing
Pillow==10.0.1
