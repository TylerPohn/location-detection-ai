# Firebase Authentication Configuration
# ===========================================================================
# Get these values from Firebase Console: https://console.firebase.google.com
# 1. Go to Project Settings → General
# 2. Scroll to "Your apps" → Select your web app
# 3. Copy the config values below

# Firebase API Key
# Location: Firebase Console → Project Settings → General → Web API Key
VITE_FIREBASE_API_KEY=AIzaSyXxXxXxXxXxXxXxXxXxXxXxXxXxXxXxXxX

# Firebase Auth Domain
# Usually: [your-project-id].firebaseapp.com
VITE_FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com

# Firebase Project ID
# Location: Firebase Console → Project Settings → General → Project ID
VITE_FIREBASE_PROJECT_ID=your-project-id

# Firebase Storage Bucket
# Usually: [your-project-id].appspot.com
VITE_FIREBASE_STORAGE_BUCKET=your-project.appspot.com

# Firebase Messaging Sender ID
# Location: Firebase Console → Project Settings → Cloud Messaging → Sender ID
VITE_FIREBASE_MESSAGING_SENDER_ID=123456789012

# Firebase App ID
# Location: Firebase Console → Project Settings → General → Your Apps → App ID
VITE_FIREBASE_APP_ID=1:123456789012:web:abcdef1234567890abcdef

# ===========================================================================
# AWS Backend Configuration
# ===========================================================================
# These are for the ML processing backend (separate from authentication)

# API Gateway URL
# Get this after deploying your AWS infrastructure
# Should look like: https://xxxxxxxxxx.execute-api.us-east-1.amazonaws.com
VITE_API_GATEWAY_URL=https://your-api-gateway.amazonaws.com

# AWS Region
# Should match your infrastructure deployment region
VITE_AWS_REGION=us-east-1

# ===========================================================================
# Optional: Demo Mode
# ===========================================================================
# Uncomment to enable demo mode (uses mock data instead of real backend)
# VITE_DEMO_MODE=false

# ===========================================================================
# INSTRUCTIONS
# ===========================================================================
# 1. Copy this file to .env.local in the same directory:
#    cp .env.local.example .env.local
#
# 2. Replace all placeholder values with your actual Firebase credentials
#
# 3. Never commit .env.local to git (it's already in .gitignore)
#
# 4. After updating .env.local, restart your dev server:
#    npm run dev
#
# 5. If you see "Firebase configuration not found", check:
#    - File is named exactly .env.local (not .env.local.txt)
#    - File is in /frontend directory
#    - All VITE_ variables are set
#    - No extra spaces or quotes around values
#    - Dev server was restarted after creating the file
#
# ===========================================================================
# FIREBASE SETUP CHECKLIST
# ===========================================================================
# Before using these credentials, make sure you've:
#
# ✅ Created a Firebase project
# ✅ Enabled Email/Password authentication
#    (Firebase Console → Authentication → Sign-in method → Email/Password → Enable)
# ✅ Added authorized domains
#    (Firebase Console → Authentication → Settings → Authorized domains → Add localhost)
# ✅ Registered a web app
#    (Firebase Console → Project Settings → General → Your apps → Add app → Web)
#
# ===========================================================================
# SECURITY NOTES
# ===========================================================================
# - VITE_FIREBASE_API_KEY is safe to expose in frontend code
#   (Firebase uses domain restrictions for security)
# - All other Firebase config values are also safe to expose
# - Never share your Firebase Admin SDK private key (used for backend)
# - For production, add your deployment domain to Firebase authorized domains
#
# ===========================================================================
