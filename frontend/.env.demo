# Demo Mode Configuration
# This file is used when running: npm run demo

# Enable demo mode with MSW (Mock Service Worker)
VITE_DEMO_MODE=true

# API base URL (not used in demo mode, requests are mocked)
VITE_API_BASE_URL=http://localhost:5173

# Demo mode uses browser-based mocks, no backend required
